# 评估报告 — 2026-05-27 本轮迭代收尾

范围：`feature/focused-conversation-memory` 上 `96388d4 → ec79a1d` 的 6 个 commit。
工作树：`.claude/worktrees/agent-a4dbe446f37a78580`。

---

## 段 1：本轮交付总览

| Commit | 一句话总结 |
|---|---|
| `56f4b32` feat: 主题系统 | 把 Aurora / Stoa 双主题、入场动效、Stoa 标签解析、build 护栏 (`plutil` + `codesign verify`) 全部从 bold-germain 分支搬回主线；ThemeProvider Combine 自动同步 `config.themeID`。 |
| `495e8c6` fix: SecureField 粘贴 | `HUDPanel.styleMask = [.fullSizeContentView]`（去掉 `.titled`）导致 paste:/copy: 选择子在响应链上路由不到 SwiftUI SecureField；`performKeyEquivalent` 显式重派发 Cmd+V/C/X/A/Z 给 NSApp。HUDPanel 和 QuickCapturePanel 都改了。 |
| `b3e9cad` feat: 三胶囊 + 多行输入 | 顶部三颗胶囊（对话 / 历史 / 设置），AppState.selectedTab 5 case 保留，MainTabView 内 `visibleTab @State` 做映射；任务 / 收件箱降级为 chat tab 上的 popover；输入框 `TextField(axis: .vertical) .lineLimit(1...6)`，Enter 发送、Shift+Enter 换行。 |
| `3859aec` fix: 热键诊断 | CarbonHotkeyManager 全链路加 OSStatus + dlog（`[CB] entry`、register OSStatus、`callbackEverFired` 标志、6s 自检 warn），目的是让下一次复发能精确定位断点。 |
| `ec79a1d` feat: Secure Input 监测 + Banner | 本轮收尾：基于运行时证据「12 分钟无操作突然好用」，断定根因是 macOS Secure Event Input，加 `HotkeyHealthMonitor` + Banner + 设置页诊断 + 一键重注册。 |

外加：本次顺手做的 `chore` 日志降级、调查日志收尾、本评估报告（合并进 `ec79a1d` 与即将提交的 `chore`）。

---

## 段 2：质量评估（按 commit）

### `56f4b32` feat: 主题系统 — **优秀**
- 双主题接入干净：`ThemeProvider.bind()` 用 Combine 订阅 `AppState.$config`，删除了 ContentView 里的冗余 onChange，避免双触发。
- `AppConfig` 自定义 Decoder 兼容旧配置：好。
- build.sh 加 `plutil -lint` + 可执行位 test + `codesign --verify`：护栏到位。
- 隐患：Stoa 标签流式解析 `StoaParser` (282 行) 没单测。一旦 MiniMax 返回的标签格式漂移会静默 fallback，难发现。
- 隐患：`ContentView.playEntrance` 用 `DispatchQueue.main.asyncAfter`，配合 `.onChange(of: isPanelVisible)` 在快速 toggle 时可能动画重叠（两个 `withAnimation` 同时跑），但不影响功能，视觉上偶有"卡一下"。

### `495e8c6` fix: SecureField 粘贴 — **合格**
- 用 `performKeyEquivalent` 重派发 Cmd+V/C/X/A/Z 是正确手法，且做了 `!option && !control` 的二次防护，不会把 Option/Control 的 chord 错误下发。
- 两个 panel (HUDPanel + QuickCapturePanel) 都覆盖到了，没漏。
- 小瑕疵：`forwardStandardEditCommands` 是 `private` global function（不在 MainActor 上）调用 `NSApp.sendAction` — 触发 Swift 6 actor isolation 警告（已存在 warning，不阻塞编译）。修复建议：把这个函数 `@MainActor` 修饰（`performKeyEquivalent` 本来就在 main）。
- 没有单测（NSEvent 这种 AppKit 路径在单测里也很难造），可接受。

### `b3e9cad` feat: 三胶囊 + 多行输入 — **合格**
- visibleTab / selectedTab 双层映射设计聪明：保留旧调用点不变，所有 popover/overlay 仍按 selectedTab 触发，但底层 visibleTab 只允许 chat/history/settings 三个。
- `onChange(of: state.selectedTab) { syncVisibleTab(from:) }` 加上 `withAnimation(AppAnimations.smooth)` 包裹，外部把 selectedTab 设成 .tasks/.inbox 时能正确把 visibleTab 拉回 chat。
- 输入框 Enter / Shift+Enter 通过 `.onKeyPress(keys: [.return], phases: .down)` 处理，逻辑清晰；并配合 onSubmit 双保险。
- 隐患：MainTabView 已有 9 条 `.animation(...)` 链式叠加，是 SwiftUI 反模式，未来加 overlay 会越来越难维护（应改为单一 `.animation(_, value:)` per 子组件，或用 transaction）。
- 隐患：`switch visibleTab { ... default: chatStack }` 的 default 分支虽然防御性写法，但 visibleTab 类型已被 `Self.visibleTabs.contains` guard，default 永远不会跑——可以删，让编译器在 enum case 增加时报错。

### `3859aec` fix: 热键诊断 — **合格**
- 全链路打点是好实践；OSStatus 显式校验 + Notion Error Logs 上报 (-9878 解释) 都到位了。
- `callbackEverFired` 是 Bool ivar、`slots: [UInt32: Slot]` 是 Dict，注册和回调都在 main，没有锁也没有数据竞争（task 3.2 复核：register 在 setupHotkey @MainActor 调用；C callback 的写 (`manager.callbackEverFired = true`) 在 Carbon 线程，读在多个地方——这是**已识别的小竞态**，但 Bool 写在 ARM/x86 上是原子的，且只是 false → true 单向，实际无害）。
- 缺点：当时打点太多（每次按键 6 条 log），见 `ec79a1d` 收尾时清理。

### `ec79a1d` feat: Secure Input 监测 + Banner — **合格**
- 设计取舍合理：Carbon callback 的 main-async hook 复用 `wasFiredBefore` 局部计算，避免多次 dispatch；HotkeyHealthMonitor 用 `Task` + `Task.sleep` 不用 Timer（task 1.2 明确要求）。
- 监视窗口：6s 初检 + 5s × 6 = 启动后 36s 内监视，足以覆盖典型 Secure Input 场景；超过 36s 还没 fire 的概率很低（用户应该已经按过热键了）。
- 设置页的 `TimelineView(.periodic(from: .now, by: 1.0))` 每秒刷新——只在设置页可见时跑（SwiftUI TimelineView 语义保证），不会持续耗电。
- 不足：当前 Banner 只在「初次自检」时推；如果用户按完热键后又陷入 Secure Input（callback 已 fire 过一次，monitor 已 stop），下一次失效不会再提示。这是已知边界——本轮的设计假设是"启动早期最容易遇到 Secure Input"。后续可以改成 IOKit `kIOHIDSystemUserHidActivityType` 或周期性 polling，但成本高、价值低，不在本轮范围。
- 小瑕疵：`HotkeyHealthMonitor.evaluate` 里有句注释「但如果 secure 由 true → false 仍然没人按热键，也不打扰用户」——目前逻辑没维护"上一次 secure"，所以 case A 的 banner 会在 8s 后自动消失，恢复时不主动推（除非 callback fire）。可接受。

---

## 段 3：技术债识别（按优先级）

### 1. [高] `AppDelegate.swift` 已经 1000+ 行，该拆了
当前包含：菜单栏、popover、HUDPanel/QuickCapturePanel 创建、热键、定时任务调度、status item 外观、UNUserNotificationCenter delegate、Space 切换响应、Cmd+= 字号调整。
建议拆为 `MenuBarController` / `PanelController` / `HotkeyController`（新增）/ `ScheduledTaskRunner`。
触发条件：下次再加一个常驻 manager 时强制拆。

### 2. [高] `ChatView.swift` 1192 行，已是项目最大文件
里面有：消息列表、流式渲染、输入框、Slash 命令补全、Voice 集成、Notion 确认卡片渲染、Stoa 卡片渲染分发、键盘事件处理。
建议拆出 `ChatInputBar`（输入框 + slash 菜单 + voice 集成，~250 行）、`ChatMessageList`（消息流，~300 行）、`ChatComposer`（顶层组合）。
和 #1 是孪生债务。

### 3. [中] `StoaParser` 282 行没有单测
流式解析 `<module>` / `<dichotomy>` / `<reflection>` 等标签——一旦 MiniMax 返回的 schema 漂移（标签换名 / 嵌套结构变化），会静默 fallback 普通 markdown，UI 上看不出来。
建议加 `StoaParserTests`，至少覆盖：单一标签、嵌套标签、未闭合标签（流式中间态）、空内容、纯文本 fallback。

### 4. [中] `forwardStandardEditCommands` 不在 `@MainActor` 上
Swift 6 编译时仍有 warning（NSApp.sendAction 跨 actor 调用）。修复：在 function 声明前加 `@MainActor`。这函数只从 `performKeyEquivalent` 调用，调用方本来就在 main。

### 5. [中] `ContentView.playEntrance` 用 DispatchQueue.main.asyncAfter，快速 toggle 时可能动画重叠
建议改用 `Task { @MainActor in try? await Task.sleep(...) }` + 一个 `currentEntranceID` 来取消旧动画。

### 6. [低] `MainTabView` 9 条链式 `.animation(_, value:)` 是 SwiftUI 反模式
单个 View 上叠这么多隐式动画修饰符，编译器会为每个变化生成独立的动画 transaction，互相之间可能竞态。
建议每个 overlay 自己用 `.transition` + `.animation` 局部声明。

### 7. [低] 设置页 Stoa / Aurora 切换没有 onAppear 重放入场动画
切主题后 ContentView 不会 re-animate（playEntrance 只在 `isPanelVisible` 变化时跑），主题色直接淡入新值，没有 burst flash 视觉强化。
不算 bug，但是体验上"主题切换有点突然"。

---

## 段 4：用户手测清单

| # | 动作 | 预期 |
|---|---|---|
| 1 | 打开 App，等 6 秒后再按 Option+Space | 面板正常弹出；屏幕顶部如果有 banner 出现说"已恢复"或没有任何 banner |
| 2 | 启动后立刻在终端跑 `sudo ls`（输完密码前），同时按 Option+Space | App 启动后 6 秒会有 banner：「系统启用了安全输入...」；终端失焦后再按 Option+Space 正常 |
| 3 | 顶部三胶囊切换：对话 → 历史 → 设置 → 对话 | 平滑切换；StatusBar / popover 只在对话 tab 出现，历史和设置 tab 上没有 |
| 4 | 在对话输入框输入两段长文本，按 Shift+Enter 换行 | 输入框增高到 6 行上限，换行成功，光标在第二行末尾 |
| 5 | 输入"/"看 slash 菜单 → 按 Enter 选第一项 | 命令执行，菜单消失（不应该意外发送 / 字符） |
| 6 | 设置页：API Key 框选中后按 Cmd+V | 剪贴板内容粘进去（这就是 495e8c6 修的） |
| 7 | 设置页 → 主题切换：Aurora ↔ Stoa | 配置保存；下次开面板入场动画用新主题；当前面板背景立刻变色（不重放 burst flash 是已知体验债） |
| 8 | 任务 popover：StatusBar 点任务图标 → 加一个任务 → 关 popover | 切到对话 tab 上叠加 popover；关闭后回到 chat |
| 9 | 收件箱 popover：StatusBar 点收件箱 | 同上，能看到随手记列表 |
| 10 | Option+\` 呼出快捷输入条 → 录音 → 按回车保存 | 输入条出现在屏幕顶部、录音转写到框中、回车后菜单栏黄点 +1 |
| 11 | 主面板入场动效：手动 Esc 关闭面板 → Option+Space 重开 | 背景从模糊→清晰、内容从下偏移 8pt → 0、burst flash 一次 |
| 12 | AddTask 浮层：点任务 popover 的"+" → 弹出 → 取消 | 仅在对话 tab 上 dim 背景 + 浮卡；切到历史 tab 时 dim 消失 |
| 13 | 设置页 → 全局热键诊断 section | "Carbon 回调已触发过" 应为 ✓（你已经按过热键）；"Secure Input" 应为 ✓ 未启用；下方 snapshot 显示 `handlerInstalled=true callbackEverFired=true count=2` |
| 14 | 设置页 → 点「重新注册热键」按钮 | 弹一条 "热键已重新注册" info banner 2.5s；按 Option+Space 仍正常工作 |
| 15 | 故意在终端跑 `sudo -k && sudo ls`，按密码提示时切到 App 设置页 | 诊断 section 的 "Secure Input" 行应实时变成 ⚠️ 已启用；终端输完密码后该行自动恢复 ✓ |

---

## 段 5：下次迭代建议

1. **`AppDelegate` 大拆分**：动手做技术债 #1 + #2。这两个文件已经是项目的"上帝对象"，每次加新功能都得在里面绕一圈。建议下一个迭代中花半天专门重构，不绑业务。

2. **Stoa / 流式解析单测补齐**：技术债 #3。把 MiniMax 在 `M2-her` 模型上的几种真实响应录下来，做 fixture-based 测试。一旦后端 schema 漂移能立刻发现。

3. **热键失效根因的"被动 + 主动"两种模式**：本轮的 monitor 是被动的（启动后短暂监测）。如果用户反馈"白天用着用着突然不行了"，可以加一个"按热键 N 次都没响应 → 弹诊断 banner"的主动模式（在 menu bar 点击事件里埋点："过去 60s 内你点过菜单栏 3 次但没按过热键？是不是热键又坏了？"）。这是产品层的可观测性，不是工程债。

---

## 验证结果

- `swift test`：76/76 通过（0.376s）
- `./build.sh`：成功，OPCCompanion.app bundle 已生成并签名
- 编译 warning：3 条 Swift 6 actor isolation warning，全部是**修复前就存在**的（NSApp 跨 actor、`forwardStandardEditCommands` 不在 main、`applicationShouldTerminate...` 的内部调用）——本轮没新增任何 warning。
