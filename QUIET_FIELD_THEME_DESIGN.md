# Quiet Field 设计稿 v2

创建时间: 2026-05-28
更新时间: 2026-05-31（v2）
状态: 前门与展开已锁，活动进出 / 意图路由待续
范围: 信息架构、入口与展开交互、主题、AI 行为规格；不含 Swift 实现细节

> **v2 相对 v1 的核心变更**
> - 入口从「菜单栏 → peek → 完整面板」改为「一根捕获 bar → 渐进展开」。peek 层取消。
> - 三胶囊导航（此刻 / 浮念 / 收束）取消。此刻是家；浮念、清扫、收束降为「进去做完退出」的活动。
> - 砍掉此刻面板上那排不挂对象的全局动词按钮（记一下 / 整理浮念 / 聊聊）。
> - 主入口 = Quick Capture 那根 bar，承载捕获，并向下卷帘展开成完整面板。
> - 旧的「聊聊」= 我想清扫会话，更名为「清扫」；「聊聊」让位给 bar 上的对话标签。

---

## 0. 一句话

**Quiet Field 不是一个更漂亮的聊天窗口，而是屏幕角落的私人思绪整理场。**

它只在四种时刻出现：

- 你正在做一件事，需要保持节奏
- 脑子里冒出东西，需要先接住
- 想法堆多了，需要分拣
- 一段工作结束，需要收束

它的高级感不来自装饰，而来自边界：少说、短句、可撤回、可归档、会闭嘴。

---

## 1. 当前产品内核

当前 OPC 已经不是普通 AI Chat。它有 6 个真实闭环：

| 闭环 | 当前能力 | 新设计里不能丢 |
|---|---|---|
| 今日焦点 | 早晨锁定任务、pinned tasks、专注计时、完成记录 | “此刻正在做什么”必须是一等信息 |
| 随手记 | Option+`、斜杠输入、Inbox、转任务、推 Notion | 捕获必须低摩擦，不污染主对话 |
| 我想清扫 | `/聊聊`、10 分钟边界、wish 决策、结束会话 | 必须显式开始和结束，不能变成长聊天 |
| 记忆 | daily note、近几天状态、长期画像、周报 | AI 应该从状态里工作，而不是每次从零问 |
| 主动介入 | 早晨遗漏、静默 30 分钟、深夜、进展 ping | 提醒必须短、有时效、点击前重检 |
| 写入确认 | Notion 写入确认、错误 banner、热键诊断 | 所有副作用必须可见、可取消、可追踪 |

Quiet Field 的设计目标不是“把这些功能重新摆一遍”，而是把它们压缩成一个更自然的生活层。

---

## 2. 核心设计原则

### 2.1 先状态，后聊天

高频入口不应该打开聊天历史。折叠门第一眼应该回答：我现在锁定了什么？我偏离了吗？脑子里还有什么没处理？下一步只需要做什么？聊天只是深入解释和执行动作的方式，不是默认主界面。

### 2.2 一根 bar，渐进展开

常态只露一根捕获 bar（外加菜单栏状态）。bar 同时是捕获入口和状态窗：上面打字、下面一行状态。只有用户明确要看更多、要对话、要整理时，bar 才向下卷帘展开成完整面板。**入口轻、展开重，但展开是同一个面板长高，不是切到另一个窗口。**

### 2.3 不把一切变成任务

OPC 的差异点不是“更强 To-Do”，而是接住未成形的东西。随手记、我想、念头、半句话、焦虑，都先进入“浮念”，再决定是否变任务。

### 2.4 每次介入都有出口

每个 AI 回合都必须有清晰收尾：已记录 / 已转任务 / 已推迟 / 已删除 / 已归档 / 不需要动作。不能留一个“你还可以继续问我”的开放尾巴。

### 2.5 隐私和副作用可见

录音、记忆、Notion 写入、长期归档都必须显示边界：存在哪里、是否发给 AI、是否写入 Notion、能否撤回或删除。bar 上「记一下」是纯本地、不走 AI；「聊聊」才发给 AI——这条边界要让用户看得见。

### 2.6 常驻 chrome 只显示状态，不显示入口

常驻可见的区域（折叠门、菜单栏、此刻 dashboard）只陈述状态，不堆功能按钮。**动作贴着它操作的那个对象出现，且只在适用时出现。** 折叠门最多 = 一根 bar + 一行状态 + 一个贴着该对象的动作（一个可以，一排不行）。这是 v2 砍冗余入口的总纲。

---

## 3. 信息架构：一个家 + 几个活动

不再用“对话 / 历史 / 设置”三胶囊作为产品叙事，也不再把此刻 / 浮念 / 收束做成三个并列的 Tab。

新模型是 **一个家 + 几个你钻进去又退出来的活动**：

| 层 | 内容 | 怎么到 |
|---|---|---|
| 家 | 折叠 bar（捕获 + 状态）↘ 展开成「此刻」面板 | 全局热键唤起 |
| 活动 · 浮念分拣 | 把堆积的浮念逐条分流 | 点状态行的「浮念 N」 |
| 活动 · 清扫 | 旧「我想清扫」，10 分钟边界、逐条四落点 | 浮念里点「清扫」 |
| 活动 · 收束 | 完成 / 深夜后的短复盘与归档 | 任务完成 / 深夜触发 |
| 控制面 | 设置 | 右上角小齿轮（低频） |

活动都是“进去 → 做完 → 显式退出 → 收回到 bar”。它们不常驻、不抢导航位。

---

## 4. 界面层级

### 4.1 菜单栏状态层

菜单栏不是入口图标，而是产品的常驻表情。它和折叠门的状态行**共用同一个状态机、同一个字符串**（见 §5.1）。

| 状态 | 菜单栏显示 |
|---|---|
| 空闲 | 静场 |
| 有任务 | 写稿 · 17:42 |
| 即将超时 | 写稿 · 4 分 |
| 有浮念 | 浮念 5 |
| 静默偏离 | 回到写稿？ |
| 深夜 | 明天再战？ |

原则：只显示一个状态、不叠多个徽章；文案不超过 8 个汉字或一个短时间值；状态过期则点击时消失，不进入过期提醒。

### 4.2 折叠门（深度 0）

全局热键唤起的第一形态。这是产品的主入口。

```
┌──────────────────────────────────┐
│  记一下 · 聊聊                     │  ← 输入模式标签（默认 记一下）
│  ✎ ____________________      ⏎   │  ← 输入框
└──────────────────────────────────┘
    下一件 · 写 PPT            ▶ 开始     ← 状态行（点这行 = 展开）
```

- 只露一根 bar + 它下面一行状态。
- 「记一下」模式：打字 + 回车 = 本地接住浮念（不走 MiniMax），bar 闪一下「已接住」后关闭，不展开。
- 「聊聊」模式：打字 → 发送时 → 向下卷帘自动展开成对话（见 §6）。
- 不聊、只想看 / 操作：点一下状态行 = 展开到「此刻」面板。
- 真空场：状态行整行消失，只剩一根 bar（见 §5.3）。

### 4.3 展开面板（深度 1）

折叠门向下卷帘后的完整形态。第一屏不是消息列表，而是「此刻」。**顶部不再有胶囊导航，bar 就是导航。**

```
┌──────────────────────────────────┐
│ 此刻  写 OPC 设计 · 17:42         │  ← dashboard（当前任务 / 计时 / 下一步 / 浮念数）
│ 下一步  把信息架构写成一页        │
│ ──────────────────────────────── │
│ （对话流，有才显示）              │
│  你: …                            │
│  整理员: 观察 / 动作 / 出口        │
│ ──────────────────────────────── │
│  ✎ 说点什么…                  ⏎  │  ← 同一根 bar，落到底部
└──────────────────────────────────┘
                              ⚙ 设置（右上角齿轮）
```

对比 v1：原来这里有一排 `[继续计时][记一下][整理浮念][聊聊]` 全局动词按钮，**v2 全部删除**。当前任务的 `完成 / 延长` 保留，因为它们贴着“正在跑的计时”这个对象；捕获走 bar；浮念走状态行里的计数；对话直接在 bar 里打字。

### 4.4 活动（深度 2）

浮念分拣 / 清扫 / 收束。进入时接管面板主体，有显式「结束 / 返回」，做完收回到 bar。进出动效应与卷帘语言呼应（具体待对齐，见 §13）。

---

## 5. 折叠门细则

### 5.1 状态行：单槽位优先级阶梯

状态行永远只显示当下最该被接住的一件事，从硬到软逐级降级，到底就是“静场”。命中第一条就停：

| 优先级 | 状态 | 状态行 | 贴着的动作 |
|---|---|---|---|
| 1 | 计时中 | `写 PPT · 17:42` | 无（时间自己跑） |
| 2 | 有待办、未开始 | `下一件 · 写 PPT`（自动顶第 1 件） | `▶ 开始 25 分` |
| 2.5 | 30 分无活动、重检仍成立 | `回到写 PPT？` | `▶ 开始` / `换一件` |
| 3 | 今早没锁定焦点 | `今天还没定 3 件事` | `立刻定` |
| 4 | 没任务、有浮念 | `浮念 5 条` | `分拣 →` |
| 5 | 深夜、今天有完成 | `今天完成 4 件 · 收束？` | `收束` |
| 6 | 真·空场 | （整行消失） | 无 |

点状态行的文字区 = 展开面板；点贴着的动作 = 执行那个动作（开始 / 分拣 / 收束…）。两者是状态行里两个不同的可点区。

### 5.2 输入模式标签（记一下 / 聊聊）

bar 上的标签表示「你打的字干嘛用」，是输入模式，**不是把功能入口塞回来**。一期只有两个：

- **记一下（默认）**：本地捕获，不走 AI，不展开，闪一下关闭。对应旧 Quick Capture。
- **聊聊**：问整理员，拿一条短回复（观察 / 动作 / 出口，见 §9）。输入发送后自动卷帘展开看对话。

约定：

- 命名：旧代码里的「聊聊」其实是“我想清扫”会话（`startWishClearingSession`），更名为「清扫」，把「聊聊」让给这里的对话标签。
- 自动展开时机：选「聊聊」→ 打字 → **发送时**才展开（不是一选就撑开，省得只想问半句也被拉开）。
- 取消隐藏快捷：不再用「回车 = 捕获 / ⌘回车 = 问 AI」这种隐藏修饰键，统一走显式标签，更好发现。
- 更聪明的意图路由（自动判断捕获 / 问 / 开任务 / 写 Notion）留作后续，见 §13。

### 5.3 真空场

阶梯到底、什么都没有时，状态行整行消失，只剩一根 bar。这是“Quiet Field / 静场”这个名字唯一被看见的瞬间——**留白本身就是产品在说话**，不填鼓励语、不填建议、不留“静场”二字。bar 的 placeholder（“记一下 · 聊聊”）已经告诉你能干嘛。

---

## 6. 展开交互与动效（卷帘揭示）

折叠门 → 展开面板的动画。核心隐喻：**输入框是你点开的把手，面板是被它拉开的房间。**

触发（三选一，无拖拽）：

- 点一下状态行的文字区
- 选「聊聊」标签并发送第一条消息（自动）
- 再按一次全局热键（= 点击展开的键盘版）

动作（卷帘揭示 / 方案 A）：

```
帧1 折叠              帧2 下拉中              帧3 展开
┌──────────────┐     ┌──────────────┐       ┌──────────────┐
│[≡ 记一下…  ⏎]│     │ 此刻 写PPT…   │  渐入  │ 此刻 写PPT·17:42│
│ 下一件·写PPT │     │ ┄┄┄┄┄┄┄┄   │       │ 下一步 …       │
└──────────────┘     │[≡ 记一下…  ⏎]│ ↓     │ ──────────── │
                     └──────────────┘       │ (对话流)       │
                                            │ ──────────── │
   bar 在顶            bar 当底轨往下滑，      │[≡ 说点什么… ⏎]│
   下面一行状态        上方内容逐行显形         └──────────────┘
                                              bar 落到底
```

- 面板顶边钉在折叠 bar 的原位；bar 当“底边轨”向下滑；上方内容（dashboard + 对话）像卷帘被拉开一样在 bar 上方逐行显形（clip 揭示，不是飞入）。
- 输入框从顶滑到底，落成“上 dashboard / 中对话流 / 下输入”的标准聊天布局。你在折叠态打了一半的字跟着 bar 一起下去，不丢。
- 状态行形变：`下一件 · 写 PPT ▶ 开始` 那行不淡出，而是“长大”升到 bar 上方，变成 dashboard。
- 时长 ≤ 240ms（首次展开可放到 280ms）。Esc 逐级反向收回。

落地分两层，逐层叠在能跑的基底上：

1. **① 离散卷帘 + 状态行干净淡出**：先把布局和窗口架构跑通（点击 / 自动 / 热键触发，卷帘揭示，状态行直接淡出、dashboard 从顶淡入）。
2. **② 升级成形变长大**：把①里的“淡出”换成状态行物理长大、升到 bar 上方的形变。

实现取向：动画跑在 SwiftUI 内；窗口是真 resize 向下长高、还是常驻“展开尺寸”只动内容，到落地时再定（拖拽已砍，不再强制“透明区点击穿透”那条路）。

---

## 7. 关键场景

### 7.1 早晨锁定

触发：第一次唤起、且今天未锁定焦点。状态行显示 `今天还没定 3 件事`，点开是“今天先定 3 件事”，三行输入 + `[锁定] [今天不定]`。完成后只显示 `已锁定 3 件事`，不继续展开长建议。

### 7.2 工作中冒出想法

触发：唤起折叠门（默认就是「记一下」）。打字 + 回车保存后 bar 立即收起。反馈：不弹大 toast，菜单栏短暂变 `已接住`，1.5 秒恢复。随手记默认不进入 AI 上下文，只有进入浮念分拣 / 清扫才批量送入 AI。

### 7.3 静默偏离

触发：有 pending task、无 active task、30 分钟无活动。状态行 / 菜单栏显示 `回到写 PPT？`。**点击前重检**：任务已完成或已有 active task → 提醒消失；仍成立 → 展开给出 `[开始 25 分钟] [换一件] [先不管]`。

### 7.4 清扫（旧“我想清扫”）

触发：浮念里点「清扫」。不是普通聊天，而是决策桌。每条 wish 必须落入一个动作：`[现在做] [转任务] [先留着] [删除]`，可填“为什么”。10 分钟边界可见。结束时汇总 `已清扫 6 条 · 转任务 2 · 留下 1 · 删除 3`，然后回到此刻。保留 `log_wish_decision` / `end_wish_clearing` 工具合同。

### 7.5 收束

触发：任务完成、计时结束、深夜提醒。短表单：“完成了什么？ / 下一步要不要留下？ `[不留] [明天继续] [转 Notion]`”。提交后只显示 `已收束`。不强迫长复盘，可跳过；已完成任务不再触发过期提醒。

### 7.6 问整理员

触发：选「聊聊」标签 → 打字 → 发送。自动卷帘展开，整理员给一条短回复（观察 / 动作 / 出口），每个回合都有出口，不留开放尾巴。

---

## 8. 视觉语言

关键词：安静、克制、私密、锋利、温纸、石墨、微光、可收起。

色彩：

| Token | 建议 |
|---|---|
| background | Graphite / Warm Paper 动态明暗 |
| primary text | 系统 primary，透明度不低于 0.92 |
| secondary text | 系统 secondary，透明度 0.65–0.72 |
| accent | Deep Vermilion，用于确认和收束 |
| signal | Warm Amber，用于浮念和待处理 |
| danger | 系统 red，只用于真实错误 |

避免：大面积紫蓝渐变、大面积纯黑、玻璃叠玻璃、装饰性水印 / 拉丁字 / 背景纹理。

字体：高频状态 SF Pro Text 13–15；当前任务 SF Pro Display semibold 16–18；复盘 / 收束 New York 少量；时间 / 诊断 SF Mono 10–12。字体变化服务于状态，不服务于“好看”。

形状：主面板圆角 10–14；折叠门 8–10；决策按钮用系统 bordered / plain，不做大胶囊；卡片只用于“待决策对象”。

动效：卷帘揭示与状态行形变是产品的签名动作。捕获 = 输入条收起 + 菜单栏闪 1 次；进入清扫 = 背景降噪、当前 wish 上浮；决策完成 = 当前 wish 向左淡出、下一条补位；收束完成 = 面板缩回菜单栏方向。除首次展开外，动效 ≤ 220ms。

---

## 9. AI 行为规格

Quiet Field 的 AI 不扮演朋友、导师、教练，它扮演“整理员”。

语气：中文、短句、不安慰、不喊口号、不做长计划、不预测未来、不把所有东西转任务。

回复合同（普通回复最多包含）：

```text
观察：一句话
动作：一个下一步
出口：记录 / 开始 / 推迟 / 删除 / 无动作
```

示例：

```text
观察：你不是没方向，是入口太多。
动作：先把 UI 方向写成一页。
出口：[开始 25 分钟]
```

禁止项：“你可以的 / 加油 / 我们来制定一个完整计划 / 这很有意义 / 我建议你先做 A 再做 B 再做 C”、未经确认写 Notion、未经确认删除用户内容、用过期状态继续提醒。

---

## 10. 技术落地边界

第一阶段不重写后端和持久化，只在现有 Theme / View / AppState 上做映射。

可复用：`ThemeID` 增加 `quietField`；`ThemeProvider` 接入新 prompt appendix；`AppState` 现有任务 / notes / wish / banner 状态继续用；`StatusBar` 作为状态机原型；`MainTabView` 改成“家 + 活动”而非三胶囊；`ChatView` 保留但降为展开面板里的对话流；`InboxView` 作为浮念分拣。

窗口架构（v2 新增）：Quick Capture 与主面板合成一个面板、两种高度，bar 是 header，展开 = 面板向下长高，废弃“两个独立 NSPanel”。热键统一为 Option+空格（Option+` 释放）。

不建议第一阶段做：接屏幕读取、持续监听、引入新依赖、重写 MiniMax tool calling、改 Notion 写入安全模型、复杂自定义图标。

---

## 11. 分阶段计划

### Phase 1 · 折叠门骨架 + 激进砍入口

- 合并窗口：单面板两高度，Option+空格 唤起折叠 bar。
- 折叠门：bar + 状态行单槽位阶梯 + 真空消失。
- 展开：离散触发（点状态行 / 热键）+ 卷帘揭示 + 状态行干净淡出（动效①）。
- 此刻面板删掉全局动词按钮排；保留当前任务 `完成 / 延长`。
- 三胶囊取消；浮念 / 收束降为活动；设置降到齿轮。

验证：`swift test` / `./build.sh` / 真实 bundle 校验；手测 Option+空格、记一下捕获、任务计时。

### Phase 2 · 输入标签 + 形变

- bar 加「记一下 / 聊聊」标签；旧“聊聊”更名“清扫”。
- 「聊聊」发送时自动展开看对话。
- 展开动效升级为状态行形变长大（动效②）。

验证：捕获不污染对话；聊聊回合都有出口；展开 / 收回不掉帧。

### Phase 3 · 活动进出 + 意图路由

- 浮念分拣 / 清扫 / 收束的进出动效（与卷帘呼应）。
- bar 意图路由：一句话自动判断捕获 / 问 / 开任务 / 写 Notion。
- 收束与归档：完成后短收束、daily note 写入可见、Notion 写入确认保持。

验证：活动有显式出口、退出后状态干净；写入永远有确认；已完成任务不再触发过期提醒。

---

## 12. 成功标准

Quiet Field 成功，不是因为截图更漂亮，而是因为：

- 唤起后第一眼知道现在该做什么，或者干脆只有一根可以打字的 bar。
- 冒出的想法可以 3 秒内被接住，不被撑开成大窗口。
- 需要更多时，面板是“同一根 bar 长出来的房间”，不是切到另一个界面。
- 提醒永远不会挡住核心操作，且每个提醒点击前都会重检。
- 「清扫」后状态一定干净退出；Notion 写入永远有确认。
- 一段工作结束后，产品会收起，而不是继续索要注意力。

---

## 13. 待对齐

- **活动进出动效**：浮念分拣 / 清扫 / 收束 接管面板与退回 bar 的具体动效，是否复用卷帘语言。
- **bar 意图路由**：超出“记一下 / 聊聊”两标签后，一句话怎么自动落到 捕获 / 问 / 开任务 / 写 Notion；以及标签与自动判断如何共存。

---

## 14. 调研来源

- Raycast AI Commands: https://manual.raycast.com/ai/ai-commands
- Raycast Pro: https://www.raycast.com/pro
- Notion Calendar menu bar: https://www.notion.com/en-gb/help/notion-calendar-settings?nxtPslug=notion-calendar-settings
- Linear Peek: https://linear.app/docs/peek
- Linear design refresh: https://linear.app/now/behind-the-latest-design-refresh
- Arc Spaces: https://resources.arc.net/hc/en-us/articles/19228064149143-Spaces-Distinct-Browsing-Areas
- Arc Auto Archive: https://resources.arc.net/hc/en-us/articles/19228855311127-Auto-Archive-Clean-as-you-go
- Superhuman Split Inbox: https://help.superhuman.com/hc/en-us/articles/38449611367187-Split-Inbox-Basics
- Wispr Flow Context Awareness: https://docs.wisprflow.ai/articles/4678293671-feature-context-awareness
- Granola 101: https://docs.granola.ai/help-center/getting-started/granola-101
- Krea Realtime: https://docs.krea.ai/user-guide/features/realtime
- Apple Liquid Glass: https://www.apple.com/ca/newsroom/2025/06/apple-introduces-a-delightful-and-elegant-new-software-design/
- ChatGPT macOS app: https://help.openai.com/en/articles/9275200-using-the-chatgpt-macos-app%23.midi
