# OPC 伴侣 - 随手记功能需求文档

> 本文档是 PRD.md 的扩展模块，专门描述"随手记"（Quick Capture）功能。
> 核心目标：**工作中临时冒出的想法，零阻力捕获，不打断心流。**

## 1. 功能定位

随手记是一个独立的轻量输入通道，**不进入主对话流**，避免污染当前任务上下文。捕获的想法进入"收件箱"，稍后统一回顾处理。

与主对话的关系：
- 主对话 = 当前任务的交互与上下文
- 随手记 = 独立收件箱，累积想法，后续整理

---

## 2. 输入方式

### 2.1 三种触发路径

| 触发方式 | 默认输入模式 | 使用场景 |
|---------|-------------|---------|
| 全局快捷键 **Option + `** | 🎤 语音自动开启 | 主要入口，心流中快速捕获 |
| 菜单栏图标右键 → "新建随手记" | ⌨️ 文字输入（不录音） | 已在鼠标操作中时使用 |
| 主面板输入框以 `/` 开头 | ⌨️ 文字输入（不录音） | 正在主对话中冒出的想法 |

### 2.2 快捷输入条 UI

按下 Option + ` 或从菜单触发时，弹出一个极简输入条：

```
┌─────────────────────────────────────────┐
│ 🎤 在说什么...                      ⏎    │
└─────────────────────────────────────────┘
```

- **位置：** 屏幕顶部中央，距离菜单栏下方 80px（不居中，避免遮挡主工作区）
- **尺寸：** 宽 520px，高 48px
- **样式：** macOS 原生毛玻璃背景，圆角 12px，无标题栏
- **行为：**
  - 只有一个输入框 + 一个发送图标（回车也可发送）
  - 弹出时有轻微从顶部滑下的动画（0.2s）
  - 点击外部 / 按 Escape → 关闭，不保存
  - 发送后立即关闭，**不显示任何 AI 回复**

### 2.3 语音模式 vs 文字模式

- **快捷键触发 → 语音模式：**
  - 输入条弹出的同时，麦克风开始录音，SFSpeechRecognizer 实时转写
  - 转写文字实时显示在输入框中
  - 按回车 / 再按一次 Option + ` → 停止录音并保存
  - 如果用户在输入框内开始打字 → 自动切换到文字模式，录音停止

- **鼠标触发 → 文字模式：**
  - 输入条弹出时不录音，光标在输入框，等待打字
  - 可以在输入框内点击麦克风图标手动开启语音

### 2.4 主面板内的 `/` 触发

在主面板的对话输入框中，当用户第一个字符输入 `/` 时：
- 输入框边框变黄色（与随手记的视觉标识一致）
- 占位提示文字变为"记一下..."
- 按回车发送时，**不会走 AI 对话流程**，直接存入收件箱
- 该条消息不出现在对话流中
- 菜单栏图标黄点 +1

---

## 3. 已发布消息转随手记

### 3.1 触发方式

鼠标悬停在对话流中任意一条消息（用户或 AI 的都可以）上超过 300ms：

```
        ┌──────────┐
        │ 📌 随手记 │  ← 浮出按钮（消息正上方）
        └────┬─────┘
             │
    ┌────────────────────────────┐
    │ [用户] 对了我应该调研下 XX  │  ← 原消息
    └────────────────────────────┘
```

- 浮出按钮位置：消息气泡正上方，间距 8px
- 按钮样式：圆角 8px，毛玻璃背景，SF Symbol `tray.and.arrow.down` + "随手记"文字
- 按钮出现动画：从消息位置向上浮出 8px + 淡入（0.15s）
- 鼠标移出消息区域 → 按钮淡出消失

### 3.2 点击后行为

1. 该消息内容被复制为一条随手记（存入收件箱）
2. 该消息从对话流中消失（有淡出+收缩动画）
3. 菜单栏图标黄点 +1
4. **如果这条消息有 AI 回复在它下方** → 相关回复也一起删除（保持对话流的连贯性）
5. **对话 session 中也要同步删除**（通过 Claude CLI 的 session 编辑，或下次调用时从上下文中排除）

### 3.3 特殊情况

- **只能转换最近 24 小时内的消息**（避免误操作旧对话）
- **系统消息（定时提醒、Notion 确认卡片等）不支持转换**

---

## 4. 反馈机制

### 4.1 菜单栏视觉反馈

| 标识 | 含义 | 颜色 |
|------|------|------|
| 🔴 红点 | 有待办提醒（已有） | 红色 |
| 🟡 黄点 | 有未处理随手记 | 黄色 |
| 数字 | 累积条数（黄点内显示） | 白色字 |

- 红点和黄点可以同时存在（分列图标两侧）
- 黄点数字达到 9+ 时显示"9+"

### 4.2 音效反馈

- 捕获成功时播放轻微 "ding" 音（使用系统音效 `Tink`）
- 可在设置页关闭
- 语音模式转写过程中不播放任何音效（避免干扰）

### 4.3 不做的事

- 不弹 toast
- 不抢焦点
- 不改变当前应用的激活状态（使用 `.nonactivatingPanel`）
- 不在主面板弹出任何提示

---

## 5. 收件箱（Inbox）

### 5.1 查看入口

在主面板底部 Tab 栏增加一个新 Tab：

```
│ 💬 对话 │ 📥 收件箱 🟡3 │ 📅 历史 │ ⚙ 设置 │
```

- 有未处理条目时，Tab 上显示黄点数字

### 5.2 收件箱界面

```
┌──────────────────────────────────────┐
│ 📥 收件箱                              │
│                                      │
│ 💡 今天（3条）                         │
│ ┌──────────────────────────────────┐ │
│ │ 对了我应该调研下 langchain       │ │
│ │ 14:32                            │ │
│ │ [完成] [转任务] [推Notion] [删除] │ │
│ └──────────────────────────────────┘ │
│ ┌──────────────────────────────────┐ │
│ │ 下周要做直播预告                  │ │
│ │ 11:20                            │ │
│ │ [完成] [转任务] [推Notion] [删除] │ │
│ └──────────────────────────────────┘ │
│                                      │
│ 💡 昨天（2条）                         │
│   ...                                │
│                                      │
│ [一键整理 →]                          │
└──────────────────────────────────────┘
```

- 按日期分组，当天的排最上面
- 每条显示内容 + 捕获时间
- 每条 4 个快捷操作：
  - **完成** → 标记已处理，条目移到已完成区
  - **转任务** → 在对话流中开启处理对话（类似"帮我把这条做成今天的任务"）
  - **推 Notion** → 写入 Notion 指定数据库（需用户在设置中配置目标数据库）
  - **删除** → 直接删除
- **一键整理** → 把所有未处理的发给 AI，让 AI 建议分类/优先级/转任务

### 5.3 数据存储

```
~/.opc-companion/inbox/
└── notes.jsonl
```

每条记录格式：
```json
{
  "id": "uuid",
  "content": "对了我应该调研下 langchain",
  "captured_at": "2026-04-15T14:32:00+08:00",
  "source": "hotkey_voice | hotkey_text | slash_in_panel | convert_from_message",
  "input_mode": "voice | text",
  "status": "pending | done | deleted",
  "processed_at": null,
  "notion_page_id": null
}
```

---

## 6. 主动提醒逻辑

### 6.1 默认规则（可在设置页调整）

**触发条件（满足任一即触发）：**
- **定时：** 每天 18:00 自动提醒
- **阈值：** 累积 ≥5 条未处理时提醒（首次达到阈值触发，之后不重复）
- **事件：** 用户完成一个计时任务后，如果有未处理随手记，轻提醒一次

### 6.2 提醒形式

不是粗暴弹窗，而是分层提醒：

1. **菜单栏图标黄点闪烁 3 次**（0.5s 周期）
2. **在对话流中插入一条系统消息：**
   ```
   💡 你今天记了 5 条随手记，要现在整理一下吗？
   [现在整理] [晚点再说]
   ```
3. 用户点「现在整理」→ 打开收件箱 Tab + AI 主动开启整理对话
4. 用户点「晚点再说」→ 2 小时后再次提醒

### 6.3 设置项

在设置页增加"随手记"板块：

```
随手记
├─ 快捷键：             [Option + `]
├─ 提醒时间：           [18:00]        [关闭]
├─ 累积阈值：           [5 条]         [关闭]
├─ 完成任务后提醒：      [开启]
├─ Notion 同步数据库：   [选择...]
├─ 捕获成功音效：        [开启]
└─ 自动删除已处理条目：   [30天后删除]
```

---

## 7. Notion 同步策略

**默认：手动同步**（避免未加工的零散想法污染 Notion）

用户在收件箱中：
- 单条推送：点「推 Notion」→ 确认目标数据库 → 推送
- 批量推送：勾选多条 → 「推送到 Notion」→ 确认

**不做自动同步**，因为：
- 随手记是粗糙的原始想法，不值得每条都进 Notion
- 用户需要先整理、归类、去重
- Notion 不是临时存储，是整理过的知识

---

## 8. 与现有功能的关系

### 8.1 不影响主对话

- 随手记**绝不**进入主对话的 Claude CLI session
- Claude CLI 在对话时看不到随手记内容
- 只有用户主动点「转任务」或「一键整理」时，相关内容才进入 AI 上下文

### 8.2 与当日任务的关系

- 随手记 ≠ 当日任务
- 随手记是**未决策的原料**，任务是**已决定要做的事**
- 通过"转任务"操作，可以把一条随手记转为当日任务

### 8.3 与定时提醒的关系

- 定时提醒针对"设定好的重复任务"（如每日复盘）
- 随手记提醒针对"累积的未处理想法"
- 两者独立，互不干扰

---

## 9. 技术实现要点

### 9.1 快捷键

- 主快捷键 Option + Space（面板）
- 随手记快捷键 Option + `（快捷输入条）
- 两个独立的 NSEvent.addGlobalMonitorForEvents 监听器

### 9.2 快捷输入条窗口

- 独立的 NSPanel（与主面板不同）
- `level: .popUpMenu`（比主面板更高）
- `styleMask: [.nonactivatingPanel, .borderless]`
- `backgroundColor: .clear` + NSVisualEffectView
- 位置计算：屏幕顶部居中，Y = 菜单栏高度 + 80px

### 9.3 消息转随手记的按钮

- 在 ChatListView 的每个消息气泡上添加 `.onHover` 修饰符
- 使用 SwiftUI 的 `.overlay` 放置浮动按钮
- `Timer` 控制 300ms 延迟显示
- 点击时触发 ChatEngine 的 `convertToNote(messageID:)` 方法

### 9.4 Claude Session 清理

消息转随手记后，需要从 Claude CLI session 中移除该消息。实现方式：
- 方案 A：删除 `~/.claude/sessions/opc-YYYY-MM-DD.json` 中对应的条目（如果 Claude CLI 允许）
- 方案 B（推荐）：维护本地"上下文排除列表"，下次调用 Claude 时用 `--resume + 新消息` 的方式，并在 system prompt 中附加"以下消息已被用户标记为无关，请忽略：[list]"
- MVP 阶段：简单实现方案 B，或直接 fork 一个新 session

---

## 10. 验收标准

### P0

- [ ] Option + ` 弹出快捷输入条，默认开启语音录音
- [ ] 鼠标从菜单栏右键触发时，输入条为文字模式（不录音）
- [ ] 主面板输入框以 `/` 开头 → 作为随手记保存，不走对话流
- [ ] 悬停消息 300ms 后，消息上方出现"随手记"按钮
- [ ] 点击按钮 → 消息转随手记 + 从对话流消失
- [ ] 菜单栏黄点 + 累积数字显示
- [ ] 收件箱 Tab，按日期分组展示所有随手记
- [ ] 每条随手记有 完成/转任务/推 Notion/删除 四个操作

### P1

- [ ] 主动提醒：18:00 + ≥5 条阈值 + 完成任务后提醒
- [ ] 提醒在对话流中以可点击卡片形式展示
- [ ] 设置页可配置所有提醒规则
- [ ] 一键整理：AI 批量建议分类/优先级

### P2

- [ ] 捕获音效
- [ ] 已处理条目自动归档/删除
- [ ] 收件箱搜索
- [ ] 随手记标签/分类
